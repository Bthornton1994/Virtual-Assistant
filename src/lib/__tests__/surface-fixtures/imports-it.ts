// The importer. Not reachable from any route, so it joins no surface set; it
// exists only to give `reachableFrom` an edge of the shape that served a claim.
export { RendersProse } from "./renders-prose.test";
